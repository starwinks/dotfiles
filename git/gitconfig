[user]
	email = 241220117@smail.nju.edu.cn
	name = starwink
[core]
	editor = nvim
[init]
	defaultBranch = main
[alias]
	lg = log --graph --abbrev-commit \
		--pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
[credential "https://github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[safe]
	directory = /home/starwink/local-shell-mcp/workspaces/default/pos-enc
	directory = /home/starwink/local-shell-mcp/workspaces/default/vibe-os
[credential]
	helper = cache
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
